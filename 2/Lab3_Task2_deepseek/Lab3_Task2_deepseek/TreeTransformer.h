#pragma once

class Node;

/**
 * Преобразует дерево: вычисляет значение поддеревьев и заменяет их листьями,
 * если результат является однозначным числом (0..9).
 * @param node корень поддерева (изменяется на месте)
 * @throws std::runtime_error при ошибках вычисления (деление на ноль и т.п.)
 */
void transformTree(Node* node);