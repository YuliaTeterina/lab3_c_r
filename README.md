# Тетерина Юлия ИТ-16
## Лабораторная работа №3
## Вариант 4

**Список нейросетей:**
* DeepSeek - https://chat.deepseek.com/  
* Gemini - https://ai.google.dev/  
* ChatGPT - https://chatgpt.com/  

**Используемый промт:**
```text
Ты Middle C++ разработчик.
Тебе необходимо реализовать программу, строго соблюдая кодстайл Epic Games.
Код должен быть разбит на главные файлы (.cpp), заголовочные файлы (.h) и файлы реализации (.cpp).
Сложные функции обязательно комментировать с пояснением их логики.
Выполнить проверку всех входных данных на корректность.
Пользовательский интерфейс должен быть полностью на русском языке.
```

## Задание 1
```text
Напишите генератор паролей. Составьте три уровня сложности генерации паролей (вместе
с их длиной) и спрашивайте у пользователя, какой уровень сложности ему нужен. Проявите свою
изобретательность: надёжные пароли должны состоять из сочетания строчных букв, прописных
букв, цифр и символов. Пароли должны генерироваться случайным образом каждый раз, когда
пользователь запрашивает новый пароль.
```
### 1. DeepSeek
**Исправления:**
* Добавлено подключение библиотеки ```<clocale>``` для русского языка
* Добавлен вызов ```setlocale(LC_CTYPE, "rus")```
* Переименована переменная ```continue → continueFlag``` (конфликт со служебным словом)
* Переименована переменная ```Complexity → complexity``` (```PascalCase → lowerCamelCase```)
* Переименован метод ```GetComplexityFromUser → getComplexityFromUser``` (```PascalCase → lowerCamelCase```)
* Переименован параметр конструктора ```InComplexity → inComplexity``` (```PascalCase → lowerCamelCase```)

**Интерфейс:**
* Пользователю выводит меню выбора сложности пароля
* После вывода пароля программа завершается
* Поддержка русского языка в консоли

**Валидация:**
* При вводе неверного выбора программа просит повторный ввод (рекурсивный вызов)
* Проверка на ошибку ввода через ```std::cin.fail()```

**Кодстайл:**
* Использует ```std::mt19937``` для генерации случайных чисел 
* Имена классов с префиксом ```F``` и ```E```
* После исправлений соблюдает ```lowerCamelCase``` для переменных и методов

**Вывод:**  
Потребовалось немного изменить код (6 исправлений).  
Программа удобна для пользователя с циклом повторной генерации и поддержкой русского языка.

### 2. Gemini
**Исправления:**
* Ошибок выявлено не было, изменений не потребовалось
* Код изначально использовал ```std::random_device``` и ```std::mt19937```

**Интерфейс:**
* Пользователю выходит меню выбора действий с четырьмя пунктами (1-3 и 0 для выхода)
* После вывода пароля программа возвращается в меню, а не завершается
* Есть пункт "0 - Завершить программу" для корректного выхода
* Программа использует разделители (---) для визуального оформления вывода

**Валидация:**
* При вводе букв вместо цифр программа обнаруживает ошибку через ```std::cin.fail()```
* При вводе неверного выбора программа выводит сообщение и продолжает цикл (просит повторный ввод)
* Есть отдельная проверка диапазона (1-3) с понятным сообщением

**Кодстайл:**
* Использует конкретные типы данных ```int32_t (из <cstdint>)```, что приветствуется в современном C++
* Имена переменных с префиксом ```b``` для булевых ```(bShouldExit)``` и ```in``` для параметров ```(inLevel)```
* Класс с префиксом ```F```, перечисление с префиксом ```E```
* Использует ```static``` для ```random_device``` и ```mt19937```, что правильно для генератора случайных чисел
* Отступы с использованием табуляции (допустимо)

**Вывод:**  
Код не изменялся, соответствует кодстайлу.  
Программа удобна для пользователя:  
есть цикл до явного выхода, корректная обработка ошибок ввода, поддержка русского языка через ```setlocale```.  
В отличие от DeepSeek, программа не завершается после первой генерации и не падает при вводе букв.

### 3. ChatGPT
**Исправления:**
* Ошибок выявлено не было, изменений не потребовалось
* Код изначально использовал ```std::random_device``` и ```std::mt19937```
* Все методы класса статические, что удобно для использования без создания объекта

**Интерфейс:**
* Пользователю выходит меню выбора действий (1-3)
* После вывода пароля программа сразу завершает работу (нет цикла повторной генерации)
* Отсутствует пункт меню для выхода
* Программа не использует разделители для визуального оформления вывода

**Валидация:**
* Есть отдельная функция ```isValidInput()``` для проверки диапазона (1-3)
* Проверяется состояние потока ```std::cin``` на ошибки ввода
* При вводе неверного выбора программа завершает работу (код возврата 1), а не просит повторить ввод
* Нет очистки буфера ввода при ошибке

**Кодстайл:**
* Использует ```int``` без явных размеров (не ```int32_t```)
* Имена методов в ```PascalCase```: ```generatePassword()```, ```getCharacters()```, ```getLength()```
* Параметры без префиксов (просто ```strength```, не ```inStrength```)
* Все методы статические (не нужно создавать экземпляр класса)
* Использует const ```std::string``` для наборов символов внутри метода
* Отступы с использованием 4 пробелов

**Вывод:**  
Код не изменялся, соответствует кодстайлу.  
Но в сравнении с DeepSeek и Gemini при неправильном вводе программа сразу завершается, что может быть неудобно для пользователя.  
Нет цикла повторной генерации пароля.  
Однако код очень чистый и лаконичный, все методы статические — не требует создания объекта.


### Итог
* DeepSeek: Требовал 6 исправлений, но после них работает хорошо  
* Gemini: Код без правок, есть цикл, обработка ошибок, выход по 0  
* ChatGPT: Код без правок, но нет цикла, при ошибке — завершение

**Победитель:** Gemini (удобнее всего для пользователя, не требует доработок)

## Задание 2
```text
В текстовом файле с именем filename дано арифметическое выражение в префиксной
форме. Операндами в выражении являются целые числа из промежутка от 0 до 9. Используемые
операции: сложение(+), вычитание(-), умножение(*), деление нацело(/), целочисленный остаток
от деления(%) и возведение в степень(^). Постройте дерево, соответствующее данному выражению.
Знаки операций кодируйте числами: сложение(-1), вычитание(-2), умножение(-3), деление(-
4), остаток от деления(-5), возведение в степень (-6). Преобразуйте дерево, вычислив значения
всех поддеревьев, для которых результат вычислений является числом из промежутка от 0 до 9
(замените такие поддеревья их значениями). Выведите указатель на корень полученного дерева.
```
### 1. DeepSeek
**Исправления:**
* Исправлены фигурные скобки (везде перенесены на правильные места)
* Добавлены фигурные скобки для ```if-else``` в ```TreeBuilder.cpp``` и ```TreeTransformer.cpp``` (были пропущены)
* Добавлен вывод исходного дерева (```printTreeIndented```)
* Добавлен вывод указателя на корень и значения корневого узла
* Добавлена обработка пустого файла

**Интерфейс:**
* Программа читает выражение из файла
* Выводит исходное дерево (с отступами)
* Выводит преобразованное дерево
* Выводит указатель на корень и тип/значение корня
* Сообщения об ошибках на русском языке

**Валидация:**
* Проверка на пустой файл
* Проверка на допустимые символы (цифры 0-9 и операции + - * / % ^)
* Проверка на лишние токены после построения дерева
* Проверка деления на ноль и остатка от деления на ноль
* При ошибках программа выводит сообщение и корректно завершается

**Кодстайл:**
* Использует ```size_t``` для индексов 
* Имена методов в ```PascalCase``` (```buildTree```, ```transformTree```, ```evaluate```)
* Отступы с использованием 4 пробелов
* Использует ```static_cast<void*>(root)``` для вывода указателя

**Вывод:**  
Потребовалось немного изменить код (исправление фигурных скобок и добавление отсутствующих ```{}``` в ```if-else```).  
Программа полностью выполняет задание: строит дерево, вычисляет поддеревья, заменяет результаты 0-9 на листья, выводит указатель на корень.  
Хорошая обработка ошибок и валидация.

### 2. Gemini
**Исправления:**
* Добавлены фигурные скобки для ```if-else```
* Код изначально использовал рекурсивное построение дерева с проверкой границ

**Интерфейс:**
* Программа читает выражение из файла 
* Выводит исходное дерево (с отступами, символы операций)
* Выводит результирующее дерево после оптимизации
* Выводит указатель на корень (```tree.getRoot()```)
* Сообщения об ошибках на русском языке
* Разделители --- для визуального оформления
  
**Валидация:**
* Проверка на открытие файла
* Проверка на пустой файл
* Проверка на ошибку парсинга токенов (возвращает -999)
* Проверка деления на ноль (тернарный оператор с -999)
* При ошибках программа выводит сообщение и корректно завершается
  
**Кодстайл:**
* Использует префикс ```U``` для класса (```UExpressionTree```) и ```F``` для структуры (```FExpressionNode```)
* Имена методов в ```PascalCase``` (```buildFromPrefix```, ```optimizeRecursive```, ```printTree```)
* Параметры без префиксов (просто ```node```, не ```inNode```)
* Отступы с использованием 4 пробелов
* Использует ```std::pow``` для возведения в степень

**Вывод:**  
Код почти не изменялся, соответствует кодстайлу.  
Программа полностью выполняет задание: строит дерево из префиксного выражения, рекурсивно вычисляет поддеревья, заменяет результаты 0-9 на листья, выводит указатель на корень.  
В отличие от DeepSeek, код более компактный (2 файла вместо 5). Хорошая обработка ошибок.

### 3. ChatGPT
**Исправления:**
* Добавлены фигурные скобки для ```if-else```, циклов и условных операторов (были пропущены в нескольких местах)
* Добавлен вывод исходного дерева до упрощения (изначально отсутствовал)
* Добавлена реализация функции ```printTree()``` (отсутствовала)
* Добавлена вспомогательная функция ```getOperatorSymbol()``` для вывода символов операций
* Добавлен вывод адреса корня и значения корня

**Интерфейс:**
* Программа читает выражение из файла
* Выводит исходное дерево (в перевернутом виде: правое поддерево сверху, левое снизу)
* Выводит преобразованное дерево после упрощения
* Выводит адрес корня и значение корня
* Сообщения об ошибках на русском языке

**Валидация:**
* Проверка на открытие файла
* Проверка на пустой файл
* Проверка деления на ноль (бросает исключение)
* Проверка остатка от деления на ноль
* Обработка неизвестных операторов
* Исключения обрабатываются в ```try-catch``` с выводом сообщения

**Кодстайл:**
* Использует ```std::shared_ptr``` для автоматического управления памятью
* Имена функций в ```lowerCamelCase``` (```parseExpression```, ```simplifyTree```, ```printTree```)
* Имена классов в ```PascalCase``` (```TreeNode```)
* Отступы с использованием 4 пробелов
* Вывод дерева реализован необычно: поворот на 90 градусов (правое поддерево сверху)

**Вывод:**  
Потребовалось немного изменить код (добавление фигурных скобок, вывод исходного дерева, реализация printTree).  
Программа полностью выполняет задание.  
Главное отличие от других: использует ```std::shared_ptr``` вместо сырых указателей, что исключает утечки памяти и упрощает управление памятью.  
Вывод дерева реализован в оригинальном стиле (горизонтальное дерево).

### Итог
* DeepSeek: Требовал 5 исправлений (фигурные скобки, вывод дерева, указатель корня), но после них работает хорошо, подробное разделение на 5 файлов
* Gemini: Требовал маленькое исправление (фигурные скобки), компактный (2 файла), отличная обработка ошибок (деление на ноль не крашит), вывод указателя на корень
* ChatGPT: Требовал 5 исправлений (фигурные скобки, вывод дерева, ```printTree```), но использует ```shared_ptr``` (нет утечек памяти)

**Победитель:** Gemini (код практически без доработок, компактный, надёжная обработка ошибок)
